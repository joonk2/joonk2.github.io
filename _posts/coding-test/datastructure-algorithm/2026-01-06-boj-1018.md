---
title:  "[백준/1018] 체스판 다시 칠하기 - 브루트포스 (Java)"
description: "백준 1018번 체스판 다시 칠하기 문제를 Java로 해결한 풀이입니다. 브루트포스 알고리즘을 활용하여 8x8 체스판 패턴을 비교하고 최소 칠하기 횟수를 구하는 방법을 설명합니다."
layout: post
categories: [coding-test, datastructure-algorithm] 
tags: [implementation]
toc: true
toc_sticky: true
math: true
date: 2026-01-06
---

# [boj_1018_체스판 다시 칠하기](https://www.acmicpc.net/problem/1018)


## 🙋‍♂️들어가며
이번 문제는 패턴을 활용한 구현이다
<br><br><br>

## 풀이
먼저 문제를 읽어보면 우리는 생각을 해볼 수 있다.

입력값에 범위가 `8 <= N, M <= 50` 이다

그렇다면 `2가지 8*8 체스판의 패턴` 활용을 통해 같지 않을때만 각 패턴이 일치하지 않는 횟수에 증가시켜 대소비교가 가능하지 않을까?


패턴은 다음과 같겠다

###  <span style="color:green">pattern_w</span>  (white 부터 시작)

|  |  |  |  |  |  |  |  |
|---|---|---|---|---|---|---|---|
| W | B | W | B | W | B | W | B |
| B | W | B | W | B | W | B | W |
| W | B | W | B | W | B | W | B |
| B | W | B | W | B | W | B | W |
| W | B | W | B | W | B | W | B |
| B | W | B | W | B | W | B | W |
| W | B | W | B | W | B | W | B |
| B | W | B | W | B | W | B | W |

###  <span style="color:red">pattern_b</span>  (Black 부터 시작)

|  |  |  |  |  |  |  |  |
|---|---|---|---|---|---|---|---|
| B | W | B | W | B | W | B | W |
| W | B | W | B | W | B | W | B |
| B | W | B | W | B | W | B | W |
| W | B | W | B | W | B | W | B |
| B | W | B | W | B | W | B | W |
| W | B | W | B | W | B | W | B |
| B | W | B | W | B | W | B | W |
| W | B | W | B | W | B | W | B |


<br>

### 만약 N, M = 10 으로, <br> 10*10 행렬인 경우, 2가지 패턴들이 아래처럼 각각 9회씩 탐색을 하겠다
![만약 N, M = 10 으로, <br> 10*10 행렬인 경우, 2가지 패턴들이 아래처럼 각각 9회씩 탐색을 하겠다 알고리즘 문제 풀이 참고 이미지](/assets/img/data-alg/implementation/2026-01-06-a.jpg)

<br><br><br>

그렇다면 코드로 보자

## 코드
```java
// 2가지 패턴을 만들어 비교한다

// 1. pattern_w (w로 먼저 시작)
// wb...
// bw...

// 2. pattern_b (b로 먼저 시작)
// bw...
// wb...

import java.io.IOException;
import java.io.InputStreamReader;
import java.io.BufferedReader;


public class Main {
	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		String[] NM = br.readLine().split(" ");
		int N = Integer.parseInt(NM[0]);
		int M = Integer.parseInt(NM[1]);
		char[][] arr = new char[N][M];
		
		for (int r = 0; r < N; r++) {
			String cols = br.readLine();
			for (int c = 0; c < M; c++) {
				arr[r][c] = cols.charAt(c);
			}
		}
		
		
		char[] p1 = {'W', 'B', 'W', 'B', 'W', 'B', 'W', 'B'};
		char[] p2 = {'B', 'W', 'B', 'W', 'B', 'W', 'B', 'W'};
		
		char[][] pattern_a = new char[8][8];
		char[][] pattern_b = new char[8][8];
		
		for (int r = 0; r < 8; r++) {
			if (r % 2 == 0) {
				for (int c = 0; c < 8; c++) {
					pattern_a[r][c] = p1[c];
					pattern_b[r][c] = p2[c];
				}
			}
			if (r % 2 == 1) {
				for (int c = 0; c < 8; c++) {
					pattern_a[r][c] = p2[c];
					pattern_b[r][c] = p1[c];
				}
			}
		}
		how_many_min_squares(N, M, arr, pattern_a, pattern_b);
	}
	
	
	
	
	
	static void how_many_min_squares(int N, int M, char[][] arr, char[][] pattern_a, char[][] pattern_b) {
		int min_square = Integer.MAX_VALUE;
		
		for (int r = 0; r < N-8 + 1; r++) {
			for (int c = 0; c < M-8 + 1; c++) {
				int cnt_a = 0;
				int cnt_b = 0;
				for (int b = r; b < r+8; b++) {
					for (int a = c; a < c+8; a++) {
						if (arr[b][a] != pattern_a[b-r][a-c]) cnt_a++;
						if (arr[b][a] != pattern_b[b-r][a-c]) cnt_b++;
					}
				}
				min_square = Math.min(min_square, Math.min(cnt_a, cnt_b));
			}
		}
		System.out.println(min_square);
	}
	
	
	
	
	
	
}
```

<br><br>

## 제출결과 -> 0.072초
![제출결과 -> 0.072초 알고리즘 문제 풀이 참고 이미지](/assets/img/data-alg/implementation/2026-01-06-b.jpg)
